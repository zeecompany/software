# AURCO Inventory Manager - runtime dependencies
PySide6>=6.6,<7
reportlab>=4.0
openpyxl>=3.1
pillow>=10.0        # required by reportlab for image support
pypdf>=4.0          # merges attachments into document PDFs
pypdfium2>=4.30     # built-in PDF studio rendering / thumbnails / image export
python-docx>=1.1    # PDF text export to Word
cryptography>=3.1   # stronger PDF encryption when available
arabic-reshaper>=3.0 # Arabic letter shaping for PDF headers
python-bidi>=0.4     # right-to-left text ordering
